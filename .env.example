# /admin Basic Auth stopgap (src/proxy.ts) — required or /admin returns 503.
# Replace with real Supabase admin-role auth once wired up (see requirements/AUDIT.md #4).
ADMIN_BASIC_AUTH_USER=
ADMIN_BASIC_AUTH_PASS=
